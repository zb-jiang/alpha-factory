1) 本地工程是一个通过LLM挖掘量化因子的工程，有完整的因子分析，因子回测流水线
2) ricequant中的脚本是在米框平台Jupiter Notbook中运行的脚本。用来验证本地的因子分析结果和米框因子分析结果一致（用来保证我本地基于tushare数据源做的因子分析结果的质量）
3) 进行本地因子分析结果和米框因子分析结果比对的时候，如果结果比对遇到不一致的情况不要凭空猜测，而是需要借助于本地的parquest过程数据和米框的过程数据做横截面比对找到差距的根因并判断是否应该应该拉齐差距（tushare和米框的原始数据有一个口径不同是已知的，tushare是“千元”+“手”， 米框是“元”+“股”。 除了这点区别外已经验证验证了多次，米框和tushare的OCHLV数据是严格能对上的）。你可以给我生成notebook脚本我手工运行完后给你米框运行的结果(请给我完整的脚本)。在本地需要运行的脚本你直接生成并直接运行，不用等我
4) 该项目的缓存机制是SQLLite数据库。按照业务逻辑缓存的使用方式是：
   - 先从缓存读取数据
   - 如果缓存命中则直接读取
   - 如果缓存没有命中则去tushare读取，并将结果保存在缓存中
   - 下次读取相同数据就会在缓存中命中
   - 